{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If memory/hot-cache.md exists in the user's project, read it silently and use it as project context for this session. If memory/open-loops.md exists and contains items with dates older than 7 days, briefly remind the user: 'You have open items from [date] — want to review them?' Do not block on this; proceed with the user's request."
          },
          {
            "type": "prompt",
            "prompt": "Check memory/audits/ for files with dates in filenames (YYYY-MM-DD format). If any audit file is older than 30 days, briefly note: 'Audit for [topic] is [X] days old — consider re-running.' Show at most 3 stale audits. Do not block on this."
          },
          {
            "type": "prompt",
            "prompt": "If memory/wiki/ exists: when hot-cache declares a project field, preferentially load memory/wiki/<project>/index.md; otherwise load memory/wiki/index.md. Read it silently and use it as a navigation aid for cross-skill queries this session. If the index has a Quick Status section with next_action items that are not empty, provide a brief, natural-language guidance message: 'Based on your previous work: [summary of pending items]. Suggested next step: [action]. Want to continue?' Do not use terms like wiki, index.md, WARM tier, ingest, or frontmatter — say 'your project records' instead. Only show guidance if there are actionable next steps; skip silently if all items are DONE. If memory/wiki/ does not exist, skip silently."
          },
          {
            "type": "prompt",
            "prompt": "If memory/wiki/ does not exist but there are 5 or more files with YAML frontmatter in the memory/ directory, and memory/wiki/ has never existed in this project (no .wiki-dismissed marker), mention once: 'You have several project records saved. I can build a summary index to help you find things faster — say \"refresh wiki index\" to get started.' Only show this hint once per session. Do not use the term wiki when speaking to the user — say 'summary index' instead. If the user declines or says they don't want an index, create an empty file memory/.wiki-dismissed to suppress future hints."
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If memory/hot-cache.md was loaded this session, keep its priorities, hero keywords, and active veto items in mind when responding. If the user's request involves a skill that uses ~~SEO tool or ~~analytics and no MCP server is connected for that category, briefly note that the skill will work in Tier 1 (manual data) mode. This is a silent reminder — do not mention it to the user unless directly relevant. Wiki query rules: (1) If the user asks a cross-skill question (e.g., 'what do we know about X', 'give me a report', 'audit status') and memory/wiki/index.md was loaded at SessionStart, always consult the index first to locate relevant files before answering — do not rely on memory alone. (2) If the user mentions a project name different from the current hot-cache project, silently check if memory/wiki/<that-project>/index.md exists and use it for that answer. (3) If the user asks about audit scores, health status, or progress, present the answer using a structured table with columns for page/item, score, health label (良好/需改进/紧急), and next action — this is the standard dashboard format."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If you just produced or substantially edited content (article, blog post, landing page, product description, meta tags), recommend running content-quality-auditor before the user publishes. Say: 'I can run a quick CORE-EEAT quality check on this content before you publish — want me to?' If the content has a target keyword, note it for the audit. Do not auto-run the audit; wait for user confirmation."
          },
          {
            "type": "prompt",
            "prompt": "If you just wrote or edited a file inside the memory/ directory (a WARM file) and memory/wiki/ already exists, silently refresh both memory/wiki/index.md (global) and the project-specific index memory/wiki/<project>/index.md if hot-cache declares a project. This is a low-risk operation — the index is a fully rebuildable derivative. Scan all memory/ files with YAML frontmatter, extract precise fields (score, status, next_action, mtime), generate best-effort summaries (max 15 chars), and write the structured index table. Do not ask for confirmation. If memory/wiki/ does not exist, skip silently — do not create it. The user must explicitly run memory-management wiki index to initialize wiki for the first time. If the refresh would take more than 3 seconds, skip silently."
          },
          {
            "type": "prompt",
            "prompt": "Phase 2 wiki: If memory/wiki/ exists and compiled pages exist within it and a WARM source file was just updated, compare the WARM file's content hash against the source hash recorded in compiled page frontmatter. If hashes differ, briefly ask: 'The wiki page for [entity/keyword] may need updating based on new data. Update it?' Wait for user confirmation before writing. If memory/wiki/ does not exist, skip silently."
          }
        ]
      }
    ],
    "FileChanged": [
      {
        "matcher": "hot-cache.md",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "hot-cache.md was just modified. Count its lines. If it exceeds 80 lines, warn the user: 'Hot cache is at [N] lines (limit: 80). Consider running memory-management to archive older entries.' If it exceeds 25KB, also warn about byte limit."
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before ending this turn: (1) If any veto-level issue was found during this session (CORE-EEAT T04, C01, R10 or CITE T03, T05, T09), append a one-line alert to memory/hot-cache.md without asking — format: YYYY-MM-DD | VETO | <item-id> | <one-line description>. (2) If this session produced SEO/GEO findings, audit results, keyword research, or other reusable data, ask the user: 'Save these results for future sessions?' If they agree, write a dated summary to the appropriate memory/ path per the state model (YYYY-MM-DD-<topic>.md)."
          },
          {
            "type": "prompt",
            "prompt": "Wiki session wrap-up: (1) If memory/wiki/index.md exists and WARM files were written or updated during this session, append a changelog entry to the bottom of index.md in the format: '| YYYY-MM-DD HH:MM | +N new, ~N updated |'. Keep only the 5 most recent entries; remove older ones. (2) Phase 2: If memory/wiki/log.md exists, append a session activity entry in the format '## [YYYY-MM-DD HH:MM] session | <project>' with sub-items for skills used, WARM files written, and wiki pages updated. If log.md exceeds 500 lines, move the oldest entries to memory/wiki/log-archive/YYYY.md (split by year of entry timestamp) until under 500 lines. If memory/wiki/ does not exist, skip both steps silently."
          }
        ]
      }
    ]
  }
}
